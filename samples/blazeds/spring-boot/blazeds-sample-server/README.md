# Sample BlazeDS Server for Feathers UI RPC Services

This is a sample server created with Java and BlazeDS that is meant to be used with the included sample BlazeDS client project.

To build and launch the server, run the following commands in this directory:

```sh
mvn clean install
java -jar target/blazeds-sample-server-1.0-SNAPSHOT-exec.war
```